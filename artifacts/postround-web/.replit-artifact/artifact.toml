kind = "web"
previewPath = "/"
title = "Post Round Coach — Marketing Site"
version = "1.0.0"
id = "artifacts/postround-web"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18644

[services.development]
run = "pnpm --filter @workspace/postround-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/postround-web", "run", "build" ]
run = "pnpm --filter @workspace/postround-web run start"

[services.env]
PORT = "18644"
BASE_PATH = "/"
