Apply one final corrective fix to the Creator Studio shareable-assets feature.

The shareable-assets implementation has already passed its API, web, and authenticated browser testing. **Do not redo or redesign the completed feature.** Preserve the existing working download/rendering architecture and make only the changes below.

## 1. Correct the product concept and terminology

These assets should **not** be called “Round Buddy quips” and should not be treated as Round Buddy messages.

Use the product concept:

**Player Notes via Round Buddy**

Round Buddy is the capture mechanism. The shareable content is the player's notable/shareable note associated with a hole.

### UI terminology

Change the Shareable Assets subsection from:

**Round Buddy**

to:

**Player Notes via Round Buddy**

Individual assets should be labeled:

* **Player Note · Hole 1**
* **Player Note · Hole 4**
* **Player Note · Hole 7**

Do not use “Round Buddy Quip” anywhere in this shareable-assets UI.

## 2. Correct the data source

The current implementation incorrectly uses:

`round_buddy_messages.content`

as the text for the downloadable assets.

Do **not** use Round Buddy conversational messages as the shareable Player Note content.

The source for Player Notes is the existing `holes` data already used by the Creator Studio round details.

Relevant `holes` schema:

```text
id
round_id
hole_number
par
score
fairway_result
gir_result
distance_to_pin_yards
putts
chip_count
bunker_shot
sand_save
scramble_opportunity
scramble_success
penalty_strokes
player_notes
ai_feedback
created_at
updated_at
playable_fairway_miss
playable_gir_miss
sand_shot_count
voice_transcript
```

### Important distinction between `player_notes` and `voice_transcript`

Use the existing `player_notes` / notable-hole qualification data to determine **which holes qualify for a shareable Player Note asset**.

However, once a hole qualifies, the downloadable image should contain the **full `voice_transcript`**, not merely the shorter `player_notes` highlight.

In other words:

**Qualification/source of notable moment:**

`holes.player_notes`

**Full text displayed in the shareable image:**

`holes.voice_transcript`

Do not substitute `round_buddy_messages.content`.

Do not regenerate or rewrite either field.

### Current test-round examples

The current Creator Studio round identifies these notable holes:

* **#1 · Eagle**

  * `player_notes`: `hold out for an eagle`
  * `voice_transcript`: `First hole, hit the fairway with my 3-wood and hold out for an eagle, no putts.`

* **#4 · Birdie**

  * `player_notes`: `Long drive, hit green, one putt for birdie.`
  * `voice_transcript`: `Long, massive drive down the middle of the fairway, hit the green, one putt for Birdie.`

* **#7 · Birdie**

  * `player_notes`: `nice approach shot, beautiful long putt`
  * `voice_transcript`: `Just missed the fairway left, nice approach shot, hit the green, beautiful long putt. 25 feet for Birdie.\n\nOne pot.`

For the downloadable Player Note images, use the **full `voice_transcript`** values above.

Do not shorten them to the `player_notes` values.

## 3. Image text must begin with the player's name

Every Player Note image should begin with:

**In the words of {display_name}:**

where `{display_name}` comes from the existing `profiles.display_name` value already used by the Creator Studio contract.

Then display the full `voice_transcript`.

For example:

**In the words of BirdieDog:**

> First hole, hit the fairway with my 3-wood and hold out for an eagle, no putts.

Use the actual `profiles.display_name` dynamically. Do not hardcode the example name.

Do not use email addresses or other profile fields.

Do not transform the player's name.

The intro should be part of the deterministic image template, not generated by AI.

## 4. Only notable holes get assets

This is **not a hole-by-hole transcript export**.

Only holes that already qualify as notable/shareable through the existing Creator Studio player-context logic **and have usable player-note/player-transcript content** should receive a downloadable Player Note asset.

For the current test round, the Shareable Assets section should contain exactly:

* Round Highlights
* Player Note · Hole 1
* Player Note · Hole 4
* Player Note · Hole 7
* Scorecard

There should be **no Player Note download buttons for holes 2, 3, 5, 6, 8, or 9**.

Do not create a new qualification algorithm.

Do not decide that every non-empty `voice_transcript` qualifies.

For example, holes 2, 6, 8, and 9 have `voice_transcript` values but are not notable Player Notes and therefore must not become shareable assets.

Reuse the existing notable-hole/Player Note qualification that is already producing the Creator Studio “Standout holes” section.

Also eliminate any duplicate entry if the source collection currently produces the same hole more than once.

## 5. Schema/API considerations

Inspect the existing Creator Studio round contract before adding anything.

The preferred architecture is:

**existing authorized Creator Studio response → qualifying hole/player-note data → local deterministic renderer**

Do not add browser-side Supabase table queries for each download.

If the existing authorized round response already contains:

* `hole_number`
* the qualification/player-note information
* `voice_transcript`
* `display_name`

use those fields directly.

If `voice_transcript` is not currently exposed in the authorized Creator Studio response, extend the existing authorized response with the **minimal structured field necessary**.

Do not expose unrelated hole/user fields.

Do not create a new client-side Supabase access path.

Do not bypass the existing Creator Studio authorization or player-approval checks.

The server-side response must continue to ensure that the requested round belongs to the authorized player/context and that existing Creator Story permission/approval rules remain satisfied.

### Do not use this table for the shareable text

`round_buddy_messages`

The previously added Round Buddy message retrieval should not become the source of Player Note image text.

If that retrieval is now unused by this feature, determine whether it can be safely removed without affecting another completed feature. Do not perform unrelated cleanup if it is still used elsewhere.

## 6. Preserve the full note in the image

The image should contain the **entire `voice_transcript` for the qualifying hole**.

Do not truncate the transcript simply because it is longer than `player_notes`.

The renderer should handle reasonable multi-line text gracefully:

* wrap text within the 1080 × 480 canvas
* preserve paragraph breaks where present
* maintain readable typography
* avoid clipping
* keep the “In the words of {display_name}:” introduction visually distinct from the note
* preserve the existing Post Round branding and overlay-friendly design

If a qualifying transcript is too long to fit cleanly, handle it through the existing deterministic text-layout/wrapping approach. Do not summarize it with AI.

## 7. Preserve the existing image behavior

Do not change the working rendering/download architecture.

Player Note assets remain:

* PNG
* 1080 × 480
* transparent / video-overlay friendly
* Post Round branded
* “Play. Learn. Share.”
* individually downloadable
* rendered only when the individual button is clicked
* no AI/image generation
* no load-time image generation
* no “Download All”

The image text should be:

**In the words of {display_name}:**

followed by the **full `voice_transcript`** from the qualifying hole.

## 8. Preserve the completed feature

Do not regress or redesign:

* Round Highlights
* existing 1080 × 1350 Scorecard export
* Creator Story behavior
* approval/permission gating
* authorized Creator Studio round response
* deterministic rendering infrastructure
* on-demand downloads
* existing Supabase/API architecture
* responsive Creator Studio behavior

Do not alter the Round Buddy capture system itself.

This is specifically a **Creator Studio terminology + data-source + qualification + image-content correction**.

## 9. Tests

Update only the tests necessary to prove this correction.

Verify:

1. Player Note assets use the existing `holes` player-context data.
2. `holes.player_notes` is used for notable/shareable-hole qualification.
3. `holes.voice_transcript` is used as the full text displayed in the image.
4. `round_buddy_messages.content` is not used as shareable Player Note text.
5. The image begins with `In the words of {display_name}:`.
6. `profiles.display_name` is sourced through the existing authorized Creator Studio path.
7. The full transcript is preserved, including multi-sentence and multi-paragraph content.
8. Only existing notable/qualifying holes produce Player Note assets.
9. The test round produces exactly Hole 1, Hole 4, and Hole 7.
10. Hole 7 is not duplicated.
11. Ordinary holes with transcripts but no qualifying Player Note do not produce assets.
12. No AI/model call is introduced.
13. No additional browser-side content request occurs when downloading.
14. Existing on-click PNG generation/download behavior remains intact.
15. Existing approval/authorization remains intact.
16. Round Highlights and Scorecard continue to pass their existing tests.

## Definition of done

Creator Studio clearly presents:

**Player Notes via Round Buddy**

with exactly the qualifying notable holes.

For the current test round:

**Player Note · Hole 1**

Image begins:

**In the words of BirdieDog:**

followed by the full Hole 1 `voice_transcript`:

`First hole, hit the fairway with my 3-wood and hold out for an eagle, no putts.`

**Player Note · Hole 4**

Image begins:

**In the words of BirdieDog:**

followed by the full Hole 4 `voice_transcript`:

`Long, massive drive down the middle of the fairway, hit the green, one putt for Birdie.`

**Player Note · Hole 7**

Image begins:

**In the words of BirdieDog:**

followed by the full Hole 7 `voice_transcript`, preserving its complete content and paragraph break.

No transcript-style assets for ordinary holes.

No Round Buddy conversational messages used as shareable text.

No AI-generated rewriting or summarization.

No changes to the already-tested Round Highlights or Scorecard functionality.
