### Task #117 — Determine PRCoachTest Test Referral URL

Inspect the existing creator referral contract and available live/read-only evidence to identify the precise referral URL for the **PRCoachTest** creator.

**Test creator**

* Display name: `PRCoachTest`
* Creator ID: `68518098-db1c-4c73-a63f-93501aebb7d5`
* Slug: `prcoachtest`
* Status: `active`
* Advanced creator: `false`

**Creator page**
`https://www.postroundcoach.com/creators/prcoachtest`

**Referral CTA target to verify**
`https://www.postroundcoach.com/r/prcoachtest`

### Objective

Determine whether `/r/prcoachtest` is the correct stable creator referral path for testing the referral flow from the PRCoachTest creator page.

Verify, using read-only inspection where possible:

1. The `prcoachtest` creator slug resolves to the correct active creator.
2. The creator referral route `/r/prcoachtest` is the correct stable referral path.
3. The route creates/reuses the creator referral link as defined by the existing referral contract.
4. Confirm that each referral visit generates a **new referral-event UUID** rather than requiring a fixed `pr_ref` UUID in the CTA.
5. Identify the expected web signup behavior, including the `pr_ref` cookie.
6. Identify the expected Android behavior:

   * If `ANDROID_STORE_URL` is configured, the referral identifier is appended to the Play Store destination.
   * If it is not configured, the route falls back to web signup.
7. Clearly distinguish:

   * the stable creator referral URL (`/r/prcoachtest`)
   * the per-visit referral-event UUID (`pr_ref`)
   * the eventual Android/Play Store destination.

### Important testing constraint

Do **not** visit the referral route during this inspection if doing so creates a referral event. This task should remain read-only unless an existing implementation contract explicitly provides a safe way to verify the route without creating referral records.

Do not create or modify referral records, creator records, application code, or configuration.

### Expected outcome

Report the exact URL to use for the temporary **Test App Referral CTA** on the PRCoachTest creator slug page.

The expected stable CTA should be:

`https://www.postroundcoach.com/r/prcoachtest`

Do not attempt to hard-code a `pr_ref` UUID into the CTA. The referral-event UUID should be generated by the referral route when the test visitor actually clicks/opens the link.

Also report any remaining limitation around verifying native mobile capture, since the mobile app source may not be available in the current workspace.
